# Optional Mem0 external memory service key for Mira.
# Copy values into /home/kenny/mira/.openclaw/secrets/memory.env on the live host.
# Never commit the live memory.env file.

MEM0_API_KEY=<set manually>
