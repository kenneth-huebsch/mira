# Copy to .openclaw/secrets/wordpress.env and replace placeholders locally.
# Keep the live file mode 600. Never commit it.
WORDPRESS_BASE_URL=https://your-wordpress-site.example
WORDPRESS_USERNAME=dedicated-mira-editor
WORDPRESS_APP_PASSWORD=replace-with-wordpress-application-password
