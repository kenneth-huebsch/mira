# Merge these names into /home/kenny/mira/.openclaw/.env on a live Mira host.
# Keep the live file ignored at mode 600; never commit the API token.
N8N_API_KEY=replace-with-n8n-api-key
N8N_BASE_URL=https://n8n.inov8.cc
